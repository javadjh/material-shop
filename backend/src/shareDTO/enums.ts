export enum SellerTypeEnum {
  seller = 'seller',
}
export enum ProduceClassEnum {
  men = 'men',
  feminine = 'feminine',
  kids = 'kids',
}
export enum produceTypeEnum {
  from2_4 = '2-4',
  from5_15 = '5-15',
  up16 = '+16',
}

export enum priorityLevelEnum {
  normal = 'normal',
  high = 'high',
  emergency = 'emergency',
}
export enum ticketStatusEnum {
  waiting_support = 'waiting-support',
  support_answered = 'support-answered',
  close = 'close',
  spam = 'spam',
}
export enum ticketSortByEnum {
  createdAt = 'createdAt',
  updatedAt = 'updatedAt',
  title = 'title',
}

export enum optionsTypeEnum {
  text = 'text',
  select = 'select',
  both = 'both',
}

export enum sellTypeEnum {
  normal = 'normal',
  promotion = 'promotion',
  festival = 'festival',
}

export enum sellerStatusEnum {
  confirm = 'confirm',
  reject = 'reject',
  waiting_for_send = 'waiting-for-send',
  waiting_for_action = 'waiting-for-action',
}
export enum productStatusEnum {
  confirmed = 'confirm',
  rejected = 'reject',
  waiting_for_send = 'waiting-for-send',
  waiting_for_action = 'waiting-for-action',
  not_complete = 'not-complete',
}

export enum targerPostPriceEnum {
  seller = 'seller',
  buyer = 'buyer',
}

export enum productActionEnum {
  reject = 'reject',
  confirm = 'confirm',
}

export enum subMarketerActionEnum {
  reject = 'reject',
  confirm = 'confirm',
}

export enum sellerActionEnum {
  reject = 'reject',
  confirm = 'confirm',
}

export enum userTypesEnum {
  seller = 'seller',
  marketer = 'marketer',
  employee = 'employee',
  buyer = 'buyer',
}

export enum shopTypeEnum {
  online = 'online',
  inPerson = 'inPerson',
  both = 'both',
}

export enum clothingStyleEnum {
  dayli = 'dayli',
  classic = 'classic',
  sport = 'sport',
}

export enum subMarketerStatusEnum {
  notComplete = 'not-complete',
  confirm = 'confirm',
  reject = 'reject',
  waitingForAction = 'waiting-for-action',
}

export enum sellOptionTypesEnum {
  color = 'color',
  size = 'size',
}
export enum buyTypeEnum {
  color = 'color',
  size = 'size',
  pack = 'pack',
}
